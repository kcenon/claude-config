# docs/.index/router.yaml
# Auto-generated by /doc-index -- DO NOT EDIT (auto sections)
# id_patterns section (bottom) is preserved across regeneration

version: "1.0"
generated: "2026-04-10T14:26:20+09:00"

# Keyword routes: map user queries to the right bundle
routes:
  - keywords: [hook, hooks, PreToolUse, PostToolUse, settings.json, permission]
    bundle: config
    primary: "HOOKS.md"
  - keywords: [commit, git, branch, merge, rebase, conflict, stash]
    bundle: workflow
  - keywords: [issue, PR, pull request, 5W1H, review]
    bundle: workflow
  - keywords: [CI, GitHub Actions, pipeline, workflow.yml]
    bundle: workflow
  - keywords: [api, REST, endpoint, route, controller, handler]
    bundle: api
  - keywords: [security, auth, OWASP, injection, XSS, encryption, secret]
    bundle: security
  - keywords: [test, testing, verification, coverage, assertion, TDD]
    bundle: project-mgmt
  - keywords: [build, CMake, Makefile, compile, link, dependency]
    bundle: project-mgmt
  - keywords: [performance, optimization, cache, latency, throughput, profiling]
    bundle: coding
  - keywords: [error, exception, handling, recovery, fallback]
    bundle: coding
  - keywords: [skill, SKILL.md, slash command, /command]
    bundle: skills-global
  - keywords: [agent, subagent, team, orchestrator, harness]
    bundle: agents
  - keywords: [plugin, extension, install, bootstrap, deploy, setup]
    bundle: config
  - keywords: [documentation, README, changelog, comment, docstring]
    bundle: project-mgmt
  - keywords: [design, prefetch, caching, module, optimization phase]
    bundle: design
  - keywords: [coding, standard, convention, naming, style, format]
    bundle: coding
  - keywords: [architecture, microservice, DDD, layer, component]
    bundle: api

# Skip rules: files that rarely need direct access
skip:
  - "plugin-lite/*"
  - "*/reference/*"
  - "*/_policy.md"

# User-defined ID pattern declarations (preserved on regeneration)
# Add patterns here, then re-run /doc-index to auto-resolve
#
# id_patterns:
#   - prefix: SRS
#     source: docs/requirements/SRS.md
#   - prefix: H
#     source: docs/safety/threat-model.md
